export class Audio {
  title: string;
  embed_url: string;
}

export class SourceAttributes {
  url:string;
  color:string;
  auto_play:boolean;
  hide_related:boolean;
  show_comments:boolean;
  show_user:boolean;
  show_reposts:boolean;
  show_teaser:boolean;
  visual:boolean;
}

export class Header {
  greeting: string;
}
